# BBDown Mobile — Nginx reverse proxy
# 两种部署方式，根据需求选一种
# ============================================================

# 方式一：独立子域名（如 bbdown.your-domain.com）
# ------------------------------------------------------------
server {
    listen 443 ssl http2;
    server_name bbdown.your-domain.com;  # REPLACE

    ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

    client_max_body_size 2048m;

    location / {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_read_timeout 86400s;
    }
}


# 方式二：挂载到已有网站的子路径（如 your-domain.com/bbdown）
# 宝塔面板操作：网站 → 伪静态 → 粘贴下面的 location 块
# ------------------------------------------------------------
# location /bbdown {
#     proxy_pass http://127.0.0.1:5001;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_set_header X-Forwarded-Prefix /bbdown;
#     proxy_buffering off;
#     proxy_read_timeout 86400s;
# }


# HTTP → HTTPS 重定向（仅方式一需要）
# ------------------------------------------------------------
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$host$request_uri;
}
